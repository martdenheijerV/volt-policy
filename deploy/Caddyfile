# Caddy reverse proxy — replace the domain with your own.
# Caddy auto-issues Let's Encrypt certs.

policy.volteuropa.org {
    encode zstd gzip
    reverse_proxy app:3000
}

supabase.policy.volteuropa.org {
    encode zstd gzip
    # Self-hosted Supabase Kong gateway (default port 8000)
    reverse_proxy host.docker.internal:8000
}

realtime.policy.volteuropa.org {
    encode zstd gzip
    reverse_proxy hocuspocus:1234
}

languagetool.policy.volteuropa.org {
    # Optional public exposure of LanguageTool — typically internal only.
    encode zstd gzip
    reverse_proxy languagetool:8010
}
